# =============================================================================
# AI Learning Assistant - Python 依赖
# Python 版本要求: >=3.10
# Conda 环境: 312_edu
#
# 安装方式:
#   conda activate 312_edu
#   pip install -r requirements.txt
# =============================================================================

# ============= 核心框架 =============
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# ============= 数据库 =============
sqlalchemy>=2.0.25
alembic>=1.13.1
aiosqlite>=0.19.0
greenlet>=3.0.3

# ============= 向量检索 & 混合检索 =============
faiss-cpu>=1.7.4
rank-bm25>=0.2.2

# ============= LangGraph & LLM =============
langgraph>=0.0.28
langchain>=0.1.0
langchain-openai>=0.0.5
langchain-anthropic>=0.1.1
langchain-community>=0.0.20
openai>=1.10.0
anthropic>=0.18.0

# ============= MCP (Model Context Protocol) =============
# Used for tool/resource servers (retrieval/ops/training-loop) and Python clients.
mcp>=1.25.0

# ============= 图像处理 =============
Pillow>=10.2.0

# ============= Embedding =============
sentence-transformers>=2.3.0
numpy>=1.26.0

# ============= HTTP & 认证 =============
httpx[socks]>=0.26.0
socksio>=1.0.0
python-multipart>=0.0.6
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.0,<5.0.0  # Pin to 4.x for Passlib compatibility
# Note: bcrypt 4.1.0+ removed __about__ attribute, compatibility patch applied in backend/core/bcrypt_compat.py
email-validator>=2.1.0

# ============= 任务队列 =============
redis>=5.0.1
celery>=5.3.6

# ============= 开发工具 =============
pytest>=7.4.4
pytest-asyncio>=0.23.3
pytest-cov>=4.1.0
black>=24.1.0
isort>=5.13.2
mypy>=1.8.0
ruff>=0.1.14
ipython>=8.20.0
ipdb>=0.13.13

# ============= 微调工具 =============
# 注意: 如需 GPU 支持，请根据 CUDA 版本安装对应的 PyTorch
# 参考: https://pytorch.org/get-started/locally/
torch>=2.1.0
transformers==4.37.2
datasets>=2.16.0
peft>=0.8.0
trl>=0.7.10
bitsandbytes>=0.42.0
accelerate>=0.26.0
tensorboard>=2.15.0
wandb>=0.16.0
pandas>=2.1.0
scikit-learn>=1.4.0

# HuggingFace Hub (explicit): used for resumable snapshot downloads.
# Keep compatible with transformers 4.37~4.39 which require huggingface-hub < 1.0.
huggingface-hub>=0.20.3,<1.0
